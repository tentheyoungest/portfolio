import React from 'react';

const SubTitleDark = (props) =>
    <React.Fragment>
        <p className='subtitle dark'>
            {props.children}
        </p>
    </React.Fragment>

export default SubTitleDark;